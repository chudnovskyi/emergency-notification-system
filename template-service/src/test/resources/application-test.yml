spring:
  datasource:
    username: test
    password: test
  jpa:
    properties.hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    topics:
      template-update: template-updated

eureka:
  client:
    enabled: false
